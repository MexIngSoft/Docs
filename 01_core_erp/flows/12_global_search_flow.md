# Flujo global de busqueda

```text
consulta -> normalizar -> detectar intencion -> buscar exacto -> buscar flexible
-> compatibilidad -> ranking -> registrar resultados
-> crear demanda si no existe resultado util
```

El Gateway central solo enruta. La API de proyecto adapta la intencion y el
Search Core ejecuta capacidades reutilizables.
